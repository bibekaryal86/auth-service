spring:
  application:
    name: user-management-system
  flyway:
    enabled: false

server:
  servlet:
    context-path: /ums

management:
  endpoints:
    web:
      exposure:
        include: '*'
        exclude: shutdown
