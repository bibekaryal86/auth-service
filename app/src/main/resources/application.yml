spring:
  application:
    name: user-management-system

server:
  servlet:
    context-path: /user-mgmt-system

management:
  endpoints:
    web:
      exposure:
        include: '*'
        exclude: shutdown
