CREATE TABLE "users" (
  "id" INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY NOT NULL,
  "first_name" VARCHAR(50) NOT NULL,
  "last_name" VARCHAR(50) NOT NULL,
  "email" VARCHAR(255) UNIQUE NOT NULL,
  "password" VARCHAR(255) NOT NULL,
  "status" VARCHAR(50) NOT NULL,
  "created" TIMESTAMP NOT NULL DEFAULT (now()),
  "updated" TIMESTAMP NOT NULL DEFAULT (now()),
  "deleted" TIMESTAMP,
  "is_superuser" boolean DEFAULT FALSE
);

CREATE TABLE "teams" (
  "id" INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY NOT NULL,
  "name" VARCHAR(100) UNIQUE NOT NULL,
  "desc" text NOT NULL,
  "status" VARCHAR(50) NOT NULL,
  "created" TIMESTAMP NOT NULL DEFAULT (now()),
  "updated" TIMESTAMP NOT NULL DEFAULT (now()),
  "deleted" TIMESTAMP
);

CREATE TABLE "roles" (
  "id" INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY NOT NULL,
  "name" VARCHAR(100) UNIQUE NOT NULL,
  "desc" text NOT NULL,
  "status" VARCHAR(50) NOT NULL,
  "created" TIMESTAMP NOT NULL DEFAULT (now()),
  "updated" TIMESTAMP NOT NULL DEFAULT (now()),
  "deleted" TIMESTAMP
);

CREATE TABLE "projects" (
  "id" INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY NOT NULL,
  "name" VARCHAR(100) UNIQUE NOT NULL,
  "desc" text NOT NULL,
  "status" VARCHAR(50) NOT NULL,
  "created" TIMESTAMP NOT NULL DEFAULT (now()),
  "updated" TIMESTAMP NOT NULL DEFAULT (now()),
  "deleted" TIMESTAMP,
  "start_date" TIMESTAMP,
  "end_date" TIMESTAMP
);

CREATE TABLE "users_audit" (
  "id" INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY NOT NULL,
  "updated_by" INTEGER NOT NULL,
  "updated_at" TIMESTAMP NOT NULL DEFAULT (now()),
  "user_id" INTEGER NOT NULL,
  "action" VARCHAR(255) NOT NULL,
  "previous" jsonb
);
ALTER TABLE "users_audit" ADD FOREIGN KEY ("updated_by") REFERENCES "users" ("id");
ALTER TABLE "users_audit" ADD FOREIGN KEY ("user_id") REFERENCES "users" ("id");

CREATE TABLE "teams_audit" (
  "id" INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY NOT NULL,
  "updated_by" INTEGER NOT NULL,
  "updated_at" TIMESTAMP NOT NULL DEFAULT (now()),
  "team_id" INTEGER NOT NULL,
  "action" VARCHAR(255) NOT NULL,
  "previous" jsonb
);
ALTER TABLE "teams_audit" ADD FOREIGN KEY ("updated_by") REFERENCES "users" ("id");
ALTER TABLE "teams_audit" ADD FOREIGN KEY ("team_id") REFERENCES "teams" ("id");

CREATE TABLE "roles_audit" (
  "id" INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY NOT NULL,
  "updated_by" INTEGER NOT NULL,
  "updated_at" TIMESTAMP NOT NULL DEFAULT (now()),
  "role_id" INTEGER NOT NULL,
  "action" VARCHAR(255) NOT NULL,
  "previous" jsonb
);
ALTER TABLE "roles_audit" ADD FOREIGN KEY ("updated_by") REFERENCES "users" ("id");
ALTER TABLE "roles_audit" ADD FOREIGN KEY ("role_id") REFERENCES "roles" ("id");

CREATE TABLE "projects_audit" (
  "id" INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY NOT NULL,
  "updated_by" INTEGER NOT NULL,
  "updated_at" TIMESTAMP NOT NULL DEFAULT (now()),
  "project_id" INTEGER NOT NULL,
  "action" VARCHAR(255) NOT NULL,
  "previous" jsonb
);
ALTER TABLE "projects_audit" ADD FOREIGN KEY ("updated_by") REFERENCES "users" ("id");
ALTER TABLE "projects_audit" ADD FOREIGN KEY ("project_id") REFERENCES "projects" ("id");

CREATE TABLE "users_teams_roles" (
  "id" INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY NOT NULL,
  "user_id" INTEGER NOT NULL,
  "team_id" INTEGER NOT NULL,
  "role_id" INTEGER NOT NULL
);
ALTER TABLE "users_teams_roles" ADD FOREIGN KEY ("user_id") REFERENCES "users" ("id");
ALTER TABLE "users_teams_roles" ADD FOREIGN KEY ("team_id") REFERENCES "teams" ("id");
ALTER TABLE "users_teams_roles" ADD FOREIGN KEY ("role_id") REFERENCES "roles" ("id");
CREATE UNIQUE INDEX "user_team_role_id" ON "users_teams_roles" ("user_id", "team_id", "role_id");

CREATE TABLE "teams_projects" (
  "id" INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY NOT NULL,
  "team_id" INTEGER NOT NULL,
  "project_id" INTEGER NOT NULL
);
ALTER TABLE "teams_projects" ADD FOREIGN KEY ("team_id") REFERENCES "teams" ("id");
ALTER TABLE "teams_projects" ADD FOREIGN KEY ("project_id") REFERENCES "projects" ("id");
CREATE UNIQUE INDEX "team_project_id" ON "teams_projects" ("team_id", "project_id");
